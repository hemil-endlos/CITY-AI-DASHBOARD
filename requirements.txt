# CITY-AI Dashboard — Django + Channels only (no torch / ultralytics / OpenCV here)
Django>=5.0,<6.0
djangorestframework>=3.15,<4.0
channels>=4.1,<5.0
daphne>=4.1,<5.0
channels-redis>=4.2,<5.0
python-dotenv>=1.0,<2.0
Pillow>=10.0,<12.0
